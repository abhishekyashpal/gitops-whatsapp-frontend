apiVersion: apps/v1
kind: Deployment
metadata:
  name: whatsapp-frontend
spec:
  replicas: 3  # ← Production has more replicas
  template:
    spec:
      containers:
      - name: whatsapp-frontend
        resources:
          limits:
            memory: "512Mi"  # ← Production gets more resources
            cpu: "1000m"